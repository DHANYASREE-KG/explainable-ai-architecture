563:function buildLayoutCandidate(
564-  land: LandDetails,
565-  facingDirection: FacingDirection,
566-  classified: ClassifiedSpace[],
567-  attachedToMap: Map<string, ClassifiedSpace>,
568-  balconyMap: Map<string, ClassifiedSpace>,
569-  config: StrategyConfig
570-): LayoutCandidate {
571-  const placements: RoomPlacement[] = [];
572-  const reasonsMap: LayoutCandidate['reasonsMap'] = {};
573-
574-  let entrance: EntranceDetails;
575-  let frontWall: 'north' | 'south' | 'east' | 'west';
576-
577-  switch (facingDirection) {
578-    case 'North':
579-      frontWall = 'north';
580-      entrance = { wall: 'north', x: land.length / 2 - 2, y: 0, width: 4, description: 'North Main Entrance' };
581-      break;
582-    case 'South':
583-      frontWall = 'south';
584-      entrance = { wall: 'south', x: land.length / 2 - 2, y: land.breadth - 0.5, width: 4, description: 'South Main Entrance' };
585-      break;
586-    case 'East':
587-      frontWall = 'east';
588-      entrance = { wall: 'east', x: land.length - 0.5, y: land.breadth / 2 - 2, width: 4, description: 'East Main Entrance' };
589-      break;
590-    case 'West':
591-      frontWall = 'west';
592-      entrance = { wall: 'west', x: 0, y: land.breadth / 2 - 2, width: 4, description: 'West Main Entrance' };
593-      break;
594-  }
595-
596-  const isPolygonPlot =
597-    land.plotType === 'polygon' &&
598-    !!land.polygonPoints &&
599-    land.polygonPoints.length >= 3;
600-
601-  const defaultSetbacks = getDefaultSetbacks(land.totalArea || land.length * land.breadth);
602-  const effectiveSetback = config.setback > 0 ? config.setback : defaultSetbacks.left;
603-
604-  const buildablePolygon = isPolygonPlot
605-    ? computeInnerBuildablePolygon(land.polygonPoints!, effectiveSetback)
606-    : null;
607-
608-  let frontSetback = defaultSetbacks.front;
609-  let rearSetback = defaultSetbacks.rear;
610-  let sideLeftSetback = defaultSetbacks.left;
611-  let sideRightSetback = defaultSetbacks.right;
612-
613-  if (config.setback > 0) {
614-    const ratio = config.setback / defaultSetbacks.left;
615-    frontSetback = Math.max(2.5, defaultSetbacks.front * ratio);
616-    rearSetback = Math.max(2.0, defaultSetbacks.rear * ratio);
617-    sideLeftSetback = config.setback;
618-    sideRightSetback = config.setback;
619-  }
620-
621-  let minX = sideLeftSetback;
622-  let maxX = land.length - sideRightSetback;
623-  let minY = rearSetback;
624-  let maxY = land.breadth - frontSetback;
625-
626-  if (facingDirection === 'North') {
627-    minY = frontSetback;
628-    maxY = land.breadth - rearSetback;
629-  } else if (facingDirection === 'South') {
630-    minY = rearSetback;
631-    maxY = land.breadth - frontSetback;
632-  } else if (facingDirection === 'East') {
633-    minX = sideLeftSetback;
634-    maxX = land.length - frontSetback;
635-    minY = sideRightSetback;
636-    maxY = land.breadth - rearSetback;
637-  } else if (facingDirection === 'West') {
638-    minX = frontSetback;
639-    maxX = land.length - sideRightSetback;
640-    minY = sideLeftSetback;
641-    maxY = land.breadth - rearSetback;
642-  }
643-
644-  const setbackBounds = { minX, maxX, minY, maxY };
645-  const occupiedBoxes: { x: number; y: number; w: number; h: number; id: string }[] = [];
646-
647-  const addReason = (
648-    id: string,
649-    constraint: string,
650-    pref: string,
651-    rejLoc: string,
652-    rejReason: string
653-  ) => {
654-    if (!reasonsMap[id]) {
655-      reasonsMap[id] = {
656-        constraints: [],
657-        preferences: [],
658-        rejected: [],
659-        tradeoffs: [],
660-      };
661-    }
662-    reasonsMap[id].constraints.push(constraint);
663-    reasonsMap[id].preferences.push(pref);
